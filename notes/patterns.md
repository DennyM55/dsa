# DSA Patterns Cheat-Sheet

Use this document to note down key patterns, their identifying characteristics, and standard templates.

## Pattern: Two Pointers
- **When to use**: Arrays or strings that are sorted or require evaluating pairs/subarrays.
- **Key Insight**: Move pointers toward each other or in the same direction to avoid O(n^2) time.
- **Template**:
  ```java
  // Example template to be filled
  ```

## Pattern: Sliding Window
- **When to use**: Finding subarrays or substrings (e.g., max sum of size k, longest substring without repeating characters).
- **Key Insight**: Maintain a window `[left, right]`. Expand `right`, and shrink `left` when a condition is violated.
- **Template**:
  ```java
  // Example template to be filled
  ```

*(Add more patterns as you learn)*
